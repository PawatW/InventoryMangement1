# PostgreSQL
DB_PASSWORD=change_me

# JWT — generate with: openssl rand -base64 32
JWT_SECRET=change_me_base64_256bit

# Cloudinary — get from your Cloudinary dashboard
CLOUDINARY_URL=cloudinary://api_key:api_secret@cloud_name
